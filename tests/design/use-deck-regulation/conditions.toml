# 検証対象: src/composables/deck/useDeckRegulation.ts
#
# tests/design/README.md の方針に基づき、実装コードを直接読んで転記した条件一覧。
# TASK-330(Tier B本展開)、Vue composableのデッキ名タグによるリミットレギュレーション状態管理。
#
# 到達不能/実質到達不能分岐:
# - なし。内部関数はreturnオブジェクトから公開され、分岐は公開APIまたはdeckInfo/getDeckNameの
#   注入状態変更で到達可能。

[meta]
feature = "use-deck-regulation"
source_file = "src/composables/deck/useDeckRegulation.ts"
verified = true
verified_at = "2026-08-07"
verified_by = "codex"
test_command = "mise run test:vitest -- tests/unit/composables/useDeckRegulation.test.ts"
source_hash = "4b38e6034c6e9601b7bcac18374293c1452b9212"
note = "verified=falseは0件"

# --- useDeckRegulation(options) ---

[[condition]]
id = "display_mode.ocg_effective_date_returns_ocg_past"
target_function = "useDeckRegulation"
source_lines = "51-55"
description = "resolvedRegulation.modeが'ocg'かつeffectiveDateがtruthyの場合、displayModeは'ocg-past'を返す"
given = "resolvedRegulation.value = { mode: 'ocg', effectiveDate: 'YYYY-MM-DD', ... }"
expect_return = "'ocg-past'"

[[condition]]
id = "display_mode.genesys_returns_genesys"
target_function = "useDeckRegulation"
source_lines = "51-55"
description = "resolvedRegulation.modeが'genesys'の場合、listParamの有無に関わらずdisplayModeは'genesys'を返す"
given = "resolvedRegulation.value.mode = 'genesys'"
expect_return = "'genesys'"

[[condition]]
id = "display_mode.none_or_ocg_latest_returns_none"
target_function = "useDeckRegulation"
source_lines = "51-55"
description = "mode='none'、またはmode='ocg'でeffectiveDateがfalsyの場合、displayModeは'none'を返す"
given = "resolvedRegulation.value.mode = 'none' または { mode: 'ocg', effectiveDate: null }"
expect_return = "'none'"

[[condition]]
id = "effective_description.none_returns_null"
target_function = "useDeckRegulation"
source_lines = "59-65"
description = "resolvedRegulation.modeが'none'の場合、effectiveDescriptionはnullを返す"
given = "resolvedRegulation.value.mode = 'none'"
expect_return = "null"

[[condition]]
id = "effective_description.fallback_yymm_preferred"
target_function = "useDeckRegulation"
source_lines = "59-65"
description = "fallback.appliedYymmがある場合、tag.yymmよりfallback.appliedYymmを優先し、modeに応じたラベルで'20YY年MM月版'文字列を返す"
given = "mode='ocg'または'genesys'、fallback.appliedYymmあり、tag.yymmもあり"
expect_return_shape = "'OCG 20YY年MM月版' または 'GENESYS 20YY年MM月版'"

[[condition]]
id = "effective_description.tag_yymm_used_without_fallback"
target_function = "useDeckRegulation"
source_lines = "59-65"
description = "fallback.appliedYymmがなくtag.yymmがある場合、tag.yymmから'20YY年MM月版'文字列を返す"
given = "mode='ocg'または'genesys'、fallbackなし、tag.yymmあり"
expect_return_shape = "'OCG 20YY年MM月版' または 'GENESYS 20YY年MM月版'"

[[condition]]
id = "effective_description_no_yymm_returns_latest"
target_function = "useDeckRegulation"
source_lines = "59-65"
description = "modeが'none'ではなく、fallback.appliedYymmもtag.yymmもない場合、modeに応じて'OCG 最新版'または'GENESYS 最新版'を返す"
given = "mode='ocg'または'genesys'、fallbackなし、tag.yymmなし"
expect_return_shape = "'OCG 最新版' または 'GENESYS 最新版'"

[[condition]]
id = "limit_override.non_ocg_or_no_effective_date_returns_null"
target_function = "useDeckRegulation"
source_lines = "72-75"
description = "getCardLimitOverrideはmodeが'ocg'でない、またはeffectiveDateがfalsyの場合、キャッシュを参照せずnullを返す"
given = "resolvedRegulation.value.mode !== 'ocg' または effectiveDate=null"
expect_return = "null"

[[condition]]
id = "limit_override.ocg_effective_date_reads_cache"
target_function = "useDeckRegulation"
source_lines = "72-75"
description = "getCardLimitOverrideはmode='ocg'かつeffectiveDateがtruthyの場合、forbiddenLimitedCache.getRegulation(cid, effectiveDate)の戻り値をそのまま返す"
given = "resolvedRegulation.value = { mode: 'ocg', effectiveDate: 'YYYY-MM-DD', ... }"
expect_return_shape = "forbiddenLimitedCache.getRegulationの戻り値"

[[condition]]
id = "genesys_point.non_genesys_returns_undefined"
target_function = "useDeckRegulation"
source_lines = "79-82"
description = "getCardGenesysPointはmodeが'genesys'でない場合、キャッシュを参照せずundefinedを返す"
given = "resolvedRegulation.value.mode !== 'genesys'"
expect_return = "undefined"

[[condition]]
id = "genesys_point.genesys_reads_cache_with_optional_list_param"
target_function = "useDeckRegulation"
source_lines = "79-82"
description = "getCardGenesysPointはmode='genesys'の場合、genesysPointCache.getPoint(cid, r.listParam ?? undefined)の戻り値をそのまま返す"
given = "resolvedRegulation.value.mode = 'genesys'"
expect_return_shape = "genesysPointCache.getPointの戻り値。listParam=null時の第2引数はundefined"

[[condition]]
id = "reset_regulation_restores_none_and_hides_dialog"
target_function = "useDeckRegulation"
source_lines = "85-88,97-98"
description = "resetRegulationはresolvedRegulationをタグ無し初期状態へ戻し、showRegulationFixDialogをfalseにする。resolveAndEnsure開始時にも同じ初期化を行う"
given = "resolvedRegulation/showRegulationFixDialogが変更済み"
expect_return_shape = "resolvedRegulation={ mode:'none', tag:null, effectiveDate:null, listParam:null, fallback:undefined } かつ showRegulationFixDialog=false"

[[condition]]
id = "resolve.ensure_order_and_available_passed_to_resolver"
target_function = "useDeckRegulation"
source_lines = "97-115"
description = "resolveAndEnsureはreset後、両キャッシュのinit、両キャッシュのcheckAndUpdateをawaitし、その後getAvailableDates/getAvailableListParamsの戻り値をavailableとしてresolveDeckRegulation(getDeckName(), available)へ渡し、戻り値をresolvedRegulationへ設定する"
given = "resolveAndEnsureを呼ぶ"
expect_return_shape = "resolveDeckRegulationに現在のデッキ名とavailableが渡され、resolvedRegulation.valueがresolver戻り値になる"

[[condition]]
id = "resolve.ocg_effective_date_ensures_forbidden_list"
target_function = "useDeckRegulation"
source_lines = "117-127"
description = "resolver戻り値がmode='ocg'かつeffectiveDateがtruthyの場合、forbiddenLimitedCache.ensureList(effectiveDate)だけを呼ぶ"
given = "resolved = { mode:'ocg', effectiveDate:'YYYY-MM-DD', listParam:null }"
expect_return_shape = "forbiddenLimitedCache.ensureList(effectiveDate)がawaitされる"

[[condition]]
id = "resolve.genesys_list_param_ensures_genesys_list"
target_function = "useDeckRegulation"
source_lines = "117-127"
description = "resolver戻り値がmode='genesys'かつlistParamがtruthyの場合、genesysPointCache.ensureList(listParam)を呼び、ensureCurrentListは呼ばない"
given = "resolved = { mode:'genesys', listParam:'YYYYMM' }"
expect_return_shape = "genesysPointCache.ensureList(listParam)がawaitされる"

[[condition]]
id = "resolve.genesys_without_list_param_ensures_current_list"
target_function = "useDeckRegulation"
source_lines = "117-127"
description = "resolver戻り値がmode='genesys'かつlistParamがfalsyの場合、genesysPointCache.ensureCurrentList()を呼ぶ"
given = "resolved = { mode:'genesys', listParam:null }"
expect_return_shape = "genesysPointCache.ensureCurrentList()がawaitされる"

[[condition]]
id = "resolve.none_or_ocg_latest_ensures_no_list"
target_function = "useDeckRegulation"
source_lines = "117-127"
description = "resolver戻り値がmode='none'、またはmode='ocg'でeffectiveDateがfalsyの場合、forbiddenLimitedCache.ensureList/genesysPointCache.ensureList/ensureCurrentListはいずれも呼ばない"
given = "resolved.mode='none' または { mode:'ocg', effectiveDate:null }"
expect_return_shape = "リストensure系メソッド呼び出しなし"

[[condition]]
id = "resolve.fallback_dialog_only_when_not_silent_not_ignored"
target_function = "useDeckRegulation"
source_lines = "129-131"
description = "resolveAndEnsureはsilent=false、resolved.fallbackあり、かつopts.dnoがignoredDeckDnosに含まれない場合だけshowRegulationFixDialogをtrueにする"
given = "opts.silent=false、resolved.fallbackあり、未ignoreのdno"
expect_return_shape = "showRegulationFixDialog=true"

[[condition]]
id = "resolve.fallback_dialog_suppressed_by_silent_missing_fallback_or_ignored"
target_function = "useDeckRegulation"
source_lines = "129-131"
description = "silent=true、fallbackなし、またはopts.dnoがignoredDeckDnosに含まれる場合、fallbackダイアログを表示しない"
given = "opts.silent=true、またはresolved.fallbackなし、またはignore済みdno"
expect_return_shape = "showRegulationFixDialog=false"

[[condition]]
id = "confirm_fix_missing_tag_or_fallback_only_hides_dialog"
target_function = "useDeckRegulation"
source_lines = "135-141"
description = "confirmRegulationFixは最初にshowRegulationFixDialogをfalseにし、resolvedRegulationにtagまたはfallbackがなければsetDeckNameを呼ばずreturnする"
given = "showRegulationFixDialog=true、tagなしまたはfallbackなし"
expect_return_shape = "showRegulationFixDialog=false、setDeckName未呼び出し"

[[condition]]
id = "confirm_fix_replaces_tag_yymm_and_sets_deck_name"
target_function = "useDeckRegulation"
source_lines = "135-141"
description = "confirmRegulationFixはtagとfallbackがある場合、replaceTagYymm(getDeckName(), r.tag, r.fallback.appliedYymm)の戻り値でsetDeckNameを呼ぶ"
given = "resolvedRegulation.valueにtagとfallbackがある"
expect_return_shape = "setDeckName(replaceTagYymmの戻り値)が呼ばれる"

[[condition]]
id = "ignore_fix_without_dno_only_hides_dialog"
target_function = "useDeckRegulation"
source_lines = "145-151"
description = "ignoreRegulationFixは最初にshowRegulationFixDialogをfalseにし、deckInfo.value.dnoがfalsyならignore集合へ追加せずstorage保存も行わない"
given = "deckInfo.value.dno = 0"
expect_return_shape = "showRegulationFixDialog=false、safeStorageSet未呼び出し"

[[condition]]
id = "ignore_fix_with_dno_adds_and_saves_string_key_record"
target_function = "useDeckRegulation"
source_lines = "145-167"
description = "ignoreRegulationFixはtruthyなdnoをignoredDeckDnosへ追加し、saveIgnoredToStorageで各dnoを文字列キー・値trueのRecordとしてCHROME_STORAGE_KEY_REGULATION_FIX_IGNOREDへ保存する"
given = "deckInfo.value.dno = 12"
expect_return_shape = "safeStorageSet({ regulation_fix_ignored: { '12': true } })がawaitされる"

[[condition]]
id = "remove_ignored_absent_returns_without_save"
target_function = "useDeckRegulation"
source_lines = "154-158"
description = "removeIgnoredは指定dnoがignoredDeckDnosに存在しない場合、削除もstorage保存も行わずreturnする"
given = "ignoredDeckDnosにdnoが存在しない"
expect_return_shape = "safeStorageSet未呼び出し"

[[condition]]
id = "remove_ignored_present_deletes_and_saves"
target_function = "useDeckRegulation"
source_lines = "154-167"
description = "removeIgnoredは指定dnoがignoredDeckDnosに存在する場合、そのdnoを削除して残りのignore集合をstorageへ保存する"
given = "ignoredDeckDnosにdnoが存在する"
expect_return_shape = "safeStorageSet({ regulation_fix_ignored: 残りのdnoだけを含むRecord })がawaitされる"

[[condition]]
id = "load_ignored_record_maps_finite_numeric_keys"
target_function = "useDeckRegulation"
source_lines = "170-180"
description = "loadIgnoredはsafeStorageGet結果のregulation_fix_ignoredが配列でないobjectの場合、Object.keys(raw).map(Number).filter(Number.isFinite)で作ったSetへignoredDeckDnosを置き換える。値は参照しない"
given = "safeStorageGetが { regulation_fix_ignored: { '12': true, abc: true, '': true, Infinity: true } } を返す"
expect_return_shape = "ignore集合は Set([12, 0]) になる"

[[condition]]
id = "load_ignored_non_record_leaves_existing_set"
target_function = "useDeckRegulation"
source_lines = "170-180"
description = "loadIgnoredはregulation_fix_ignoredがobjectでない、null、または配列の場合、ignoredDeckDnosを変更しない"
given = "safeStorageGet結果のregulation_fix_ignoredが配列またはnull"
expect_return_shape = "既存のignore集合が維持される"

[[condition]]
id = "watch_tag_raw_same_returns_without_resolve"
target_function = "useDeckRegulation"
source_lines = "183-192"
description = "currentTagRawのwatchはnewRawとoldRawが同じ場合、dno確認やresolveAndEnsure呼び出しをせずreturnする"
given = "デッキ名は変わるがparseRegulationTag(getDeckName())?.rawが同じ"
expect_return_shape = "resolveAndEnsure由来のcache init/check呼び出しなし"

[[condition]]
id = "watch_tag_raw_changed_without_dno_returns_without_resolve"
target_function = "useDeckRegulation"
source_lines = "183-192"
description = "currentTagRawのwatchはタグrawが変わってもdeckInfo.value.dnoがfalsyの場合、resolveAndEnsureを呼ばずreturnする"
given = "newRaw !== oldRaw、deckInfo.value.dno=0"
expect_return_shape = "resolveAndEnsure由来のcache init/check呼び出しなし"

[[condition]]
id = "watch_tag_raw_changed_with_dno_resolves_silent"
target_function = "useDeckRegulation"
source_lines = "183-192"
description = "currentTagRawのwatchはタグrawが変わりdeckInfo.value.dnoがtruthyの場合、resolveAndEnsure({ dno, silent: true })をawaitする"
given = "newRaw !== oldRaw、deckInfo.value.dno=12"
expect_return_shape = "resolverに新デッキ名が渡り、fallbackがあってもshowRegulationFixDialogはtrueにならない"
